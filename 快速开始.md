# 快速开始（下一步）

## 1. 安装依赖

```bash
pip install -r requirements.txt
```

## 2. 校验数据

```bash
python validate_data.py --file data/train.jsonl
python validate_data.py --file data/val.jsonl
```

## 3. 开始训练

```bash
python train.py --config train_config.yaml
```

## 4. 关键文件

- 训练脚本：`train.py`
- 数据校验：`validate_data.py`
- 训练配置：`train_config.yaml`
- 数据格式约束：`data_schema.json`
- 训练数据：`data/train.jsonl`
- 验证数据：`data/val.jsonl`

## 5. OOM 调参顺序

1. 先降 `max_length`：`1536 -> 1024`
2. 再降 `lora.r`：`64 -> 32`
3. 最后降有效 batch（减少 `gradient_accumulation_steps`）
